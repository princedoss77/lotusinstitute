# Workers: `npx wrangler deploy` serves files from ./public as static assets.
# Cloudflare Pages (Git): set “Build output directory” to `public` (no build command).

name = "lotusinstitute"
compatibility_date = "2026-04-01"

[assets]
directory = "public"
